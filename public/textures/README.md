# public/textures

Drop texture PNG/WebP files here, then register them in
`src/components/engram/textures.ts` (TEXTURE_MANIFEST). See `docs/ART_ASSETS.md`
for the slot list and specs. Until a file is registered, the scene uses its
generic flat-color fallback.
